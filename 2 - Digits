#include <stdio.h>

int main(){
    long long n;
    short int count = 0;
    scanf("%lld", &n);
    if (n == 0) count = 1;
    while (n > 0){
        count++;
        n /= 10;
    }
    printf("%hd", count);
    return 0;
}
