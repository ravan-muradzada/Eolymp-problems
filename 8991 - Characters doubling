#include <iostream>
#include <string>
using namespace std;

int main(){
    string sentence, newSentence = "";
    getline(cin, sentence);

    for (char i: sentence){
        newSentence += i;
        if (isalpha(i)) newSentence += i;
    }
    cout << newSentence;
    

    return 0;
}
